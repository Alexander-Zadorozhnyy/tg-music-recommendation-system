# Music Search API

Полнофункциональный FastAPI сервис для семантического и текстового поиска по музыкальному индексу CEDS (Circumplex of Emotions in Digital Songs).

## 📋 Содержание

- [Описание](#описание)
- [Архитектура](#архитектура)
- [Компоненты системы](#компоненты-системы)
- [Установка и настройка](#установка-и-настройка)
- [Запуск](#запуск)
- [API Документация](#api-документация)
- [Примеры использования](#примеры-использования)
- [Конфигурация](#конфигурация)
- [Разработка](#разработка)

## Описание

Music Search API предоставляет RESTful интерфейс для поиска по индексированной коллекции музыкальных треков. Сервис поддерживает:

- **Текстовый поиск** с поддержкой ngram для частичных совпадений
- **Мультиполевой поиск** по артистам, названиям треков и текстам песен
- **Фильтрацию** по жанру, теме, году выпуска и аудио-характеристикам
- **BM25 ранжирование** результатов поиска
- **Полную документацию** через Swagger UI и ReDoc

## Архитектура

Сервис построен на принципах **чистой архитектуры** с разделением на слои:

```
┌─────────────────────────────────────────┐
│         FastAPI Application             │
│         (app/main.py)                   │
└─────────────────────────────────────────┘
                    │
        ┌───────────┴───────────┐
        │                       │
┌───────▼────────┐    ┌─────────▼────────┐
│   Routers      │    │   Middleware    │
│  (endpoints)   │    │   (CORS, etc)   │
└───────┬────────┘    └─────────────────┘
        │
┌───────▼─────────────────────────────────┐
│         Services Layer                   │
│  ┌──────────────────────────────────┐   │
│  │   SearchService                  │   │
│  │   (Бизнес-логика поиска)          │   │
│  └──────────────┬───────────────────┘   │
│                 │                        │
│  ┌──────────────▼───────────────────┐   │
│  │   OpenSearchService              │   │
│  │   (Работа с OpenSearch)          │   │
│  └──────────────────────────────────┘   │
└──────────────────────────────────────────┘
                    │
        ┌───────────▼───────────┐
        │   OpenSearch          │
        │   (music_ceds_semantic)│
        └───────────────────────┘
```

### Принципы проектирования

1. **Separation of Concerns** - каждый компонент отвечает за свою область
2. **Dependency Injection** - сервисы создаются как синглтоны
3. **Type Safety** - использование Pydantic для валидации данных
4. **Error Handling** - централизованная обработка ошибок

## Компоненты системы

### 1. Конфигурация (`app/config.py`)

Централизованное управление настройками приложения через класс `Settings`:

```python
class Settings:
    OPENSEARCH_URL: str
    OPENSEARCH_USER: str
    OPENSEARCH_INITIAL_ADMIN_PASSWORD: str
    YANDEX_API_KEY: str
    YANDEX_FOLDER_ID: str
    INDEX_NAME: str
    # ... и другие настройки
```

**Особенности:**

- Загрузка из переменных окружения
- Значения по умолчанию для разработки
- Типизированные настройки

### 2. Модели данных (`app/models/schemas.py`)

Pydantic модели для валидации запросов и ответов:

#### `SearchRequest`

Модель запроса на поиск:

- `query` - поисковый запрос (обязательное поле)
- `size` - количество результатов (1-100, по умолчанию 10)
- `search_fields` - поля для поиска (опционально)
- `filters` - фильтры для уточнения поиска

#### `SearchResponse`

Модель ответа на поисковый запрос:

- `total` - общее количество найденных результатов
- `results` - список треков (`TrackResult`)
- `query` - исходный запрос
- `took` - время выполнения в миллисекундах

#### `TrackResult`

Информация о треке:

- Основные поля: `track_id`, `artist_name`, `track_name`, `genre`, `release_date`, `topic`
- Текст песни: `lyrics`
- Аудио-характеристики: `danceability`, `energy`, `valence`, `acousticness`, `instrumentalness`, `loudness`
- Релевантность: `score`

### 3. Сервисы

#### `OpenSearchService` (`app/services/opensearch_service.py`)

Низкоуровневый сервис для работы с OpenSearch:

**Основные методы:**

- `index_exists()` - проверка существования индекса
- `get_document_count()` - получение количества документов
- `search()` - выполнение поискового запроса
- `get_track_by_id()` - получение трека по ID

**Особенности:**

- Управление подключением к OpenSearch
- Построение сложных поисковых запросов
- Обработка фильтров и агрегаций
- Обработка ошибок подключения

#### `SearchService` (`app/services/search_service.py`)

Высокоуровневый сервис для поиска:

**Основные методы:**

- `search()` - выполнение поиска с преобразованием результатов
- `get_track()` - получение трека по ID

**Особенности:**

- Преобразование ответов OpenSearch в Pydantic модели
- Бизнес-логика поиска
- Обработка и форматирование результатов

### 4. Роутеры

#### `search.py` - API для поиска

**Endpoints:**

1. **POST /search** - Поиск через POST запрос

   ```json
   {
     "query": "love",
     "size": 10,
     "search_fields": ["artist_name", "track_name", "lyrics"],
     "filters": {
       "genre": "pop",
       "release_date": {"gte": 2000}
     }
   }
   ```

2. **GET /search** - Поиск через GET запрос (удобно для тестирования)

   ```
   GET /search?q=love&size=10&genre=pop&year_from=2000
   ```

3. **GET /search/track/{track_id}** - Получить трек по ID

#### `health.py` - Проверка здоровья сервиса

**GET /health** - Проверка состояния:

- Статус сервиса
- Существование индекса
- Количество документов в индексе

### 5. Главное приложение (`app/main.py`)

FastAPI приложение с настройкой:

- CORS middleware
- Подключение роутеров
- Конфигурация документации

## Установка и настройка

### Требования

- Python 3.8+
- OpenSearch 2.x
- Доступ к Yandex Cloud API (опционально, для эмбеддингов)

### Установка зависимостей

```bash
pip install -r requirements.txt
```

**Зависимости:**

- `fastapi` - веб-фреймворк
- `uvicorn` - ASGI сервер
- `pydantic` - валидация данных
- `opensearch-py` - клиент OpenSearch
- `requests` - HTTP клиент
- `python-dotenv` - загрузка переменных окружения

### Настройка переменных окружения

**⚠️ ВАЖНО:** Все секретные данные (пароли, API ключи) должны быть настроены через переменные окружения или `.env` файл. Они не должны быть в коде!

Создайте файл `.env` в корне проекта:

```bash
# OpenSearch (ОБЯЗАТЕЛЬНО)
OPENSEARCH_URL=https://localhost:9200
OPENSEARCH_USER=your_opensearch_user
OPENSEARCH_INITIAL_ADMIN_PASSWORD=your_opensearch_password

# Yandex API (ОБЯЗАТЕЛЬНО)
YANDEX_API_KEY=your_yandex_api_key
YANDEX_FOLDER_ID=your_yandex_folder_id

# Индекс (опционально)
OPENSEARCH_INDEX=music_ceds_semantic

# Отладка (опционально)
DEBUG=False
```

Или экспортируйте переменные окружения:

```bash
export OPENSEARCH_URL="https://localhost:9200"
export OPENSEARCH_USER="your_user"
export OPENSEARCH_INITIAL_ADMIN_PASSWORD="your_password"
export YANDEX_API_KEY="your_key"
export YANDEX_FOLDER_ID="your_folder_id"
```

**Примечание:** Если настройки не заполнены, приложение не запустится и выдаст понятное сообщение об ошибке.

## Запуск

### Режим разработки

```bash
# Через скрипт
./run.sh

# Или напрямую
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

**Особенности режима разработки:**

- Автоперезагрузка при изменении кода
- Подробные логи ошибок
- Swagger UI доступен по `/docs`

### Продакшн режим

```bash
uvicorn app.main:app \
  --host 0.0.0.0 \
  --port 8000 \
  --workers 4 \
  --log-level info
```

**Рекомендации для продакшна:**

- Используйте reverse proxy (nginx)
- Настройте SSL/TLS
- Ограничьте CORS origins
- Используйте переменные окружения для секретов
- Настройте мониторинг и логирование

### Docker (опционально)

Создайте `Dockerfile`:

```dockerfile
FROM python:3.10-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY app/ ./app/
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

## API Документация

После запуска сервиса документация доступна:

- **Swagger UI**: <http://localhost:8000/docs>
  - Интерактивная документация
  - Возможность тестирования API
  - Описание всех endpoints и моделей

- **ReDoc**: <http://localhost:8000/redoc>
  - Альтернативная документация
  - Удобное чтение спецификации

- **OpenAPI Schema**: <http://localhost:8000/openapi.json>
  - JSON схема API
  - Для интеграции с другими инструментами

## Примеры использования

### Python

#### Базовый поиск

```python
import requests

response = requests.post(
    "http://localhost:8000/search",
    json={
        "query": "love",
        "size": 10
    }
)

data = response.json()
print(f"Найдено: {data['total']} результатов")
for track in data['results']:
    print(f"{track['artist_name']} - {track['track_name']}")
```

#### Поиск с фильтрами

```python
response = requests.post(
    "http://localhost:8000/search",
    json={
        "query": "love",
        "size": 20,
        "filters": {
            "genre": "pop",
            "release_date": {"gte": 2000, "lte": 2020},
            "energy": {"gte": 0.7}
        }
    }
)
```

#### Поиск по конкретным полям

```python
response = requests.post(
    "http://localhost:8000/search",
    json={
        "query": "beatles",
        "size": 10,
        "search_fields": ["artist_name", "track_name"]
    }
)
```

#### Получение трека по ID

```python
response = requests.get("http://localhost:8000/search/track/123")
track = response.json()
print(track['lyrics'])
```

### cURL

#### POST запрос

```bash
curl -X POST "http://localhost:8000/search" \
  -H "Content-Type: application/json" \
  -d '{
    "query": "love",
    "size": 10,
    "filters": {
      "genre": "pop"
    }
  }'
```

#### GET запрос

```bash
curl "http://localhost:8000/search?q=love&size=10&genre=pop&year_from=2000"
```

#### Проверка здоровья

```bash
curl "http://localhost:8000/health"
```

### JavaScript/TypeScript

```javascript
// Поиск
const response = await fetch('http://localhost:8000/search', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    query: 'love',
    size: 10,
    filters: {
      genre: 'pop'
    }
  })
});

const data = await response.json();
console.log(`Найдено: ${data.total} результатов`);
data.results.forEach(track => {
  console.log(`${track.artist_name} - ${track.track_name}`);
});
```

## Конфигурация

### Поддерживаемые фильтры

#### Текстовые фильтры (точное совпадение)

- `genre` - жанр музыки

  ```json
  {"genre": "pop"}
  ```

- `topic` - тема песни

  ```json
  {"topic": "romantic"}
  ```

#### Числовые фильтры

- `release_date` - год выпуска

  ```json
  {"release_date": 2000}  // Точное значение
  {"release_date": {"gte": 2000, "lte": 2020}}  // Диапазон
  ```

- `danceability` - танцевальность (0.0 - 1.0)

  ```json
  {"danceability": {"gte": 0.7}}
  ```

- `energy` - энергия (0.0 - 1.0)

  ```json
  {"energy": {"gte": 0.8}}
  ```

- `valence` - валентность (0.0 - 1.0)

  ```json
  {"valence": {"gte": 0.6}}
  ```

- `acousticness` - акустичность (0.0 - 1.0)

  ```json
  {"acousticness": {"gte": 0.5}}
  ```

### Поля поиска

По умолчанию поиск выполняется по всем полям:

- `artist_name` - имя артиста (boost: 2.0)
- `track_name` - название трека (boost: 2.0)
- `lyrics` - текст песни (boost: 3.0)

Можно указать конкретные поля:

```json
{
  "query": "beatles",
  "search_fields": ["artist_name", "track_name"]
}
```

### Особенности поиска

1. **Ngram поиск** - поддержка частичных совпадений
   - Поиск "beat" найдет "beatles", "beats", "heartbeat"

2. **Fuzzy поиск** - исправление опечаток
   - Поиск "beatles" найдет "beatles" даже при опечатке

3. **Multi-match** - поиск по нескольким полям одновременно
   - Автоматическое объединение результатов

4. **BM25 ранжирование** - релевантность результатов
   - Учитывается частота терминов
   - Учитывается длина документа

## Разработка

### Структура проекта

```md
search_service/
├── app/
│   ├── __init__.py
│   ├── main.py              # FastAPI приложение
│   ├── config.py            # Конфигурация
│   ├── models/
│   │   ├── __init__.py
│   │   └── schemas.py       # Pydantic модели
│   ├── services/
│   │   ├── __init__.py
│   │   ├── opensearch_service.py
│   │   └── search_service.py
│   └── routers/
│       ├── __init__.py
│       ├── search.py
│       └── health.py
├── requirements.txt
├── README.md
└── run.sh
```

### Добавление новых endpoints

1. Создайте функцию в соответствующем роутере (`app/routers/`)
2. Используйте Pydantic модели для валидации
3. Вызывайте сервисы для бизнес-логики
4. Документируйте через docstrings

Пример:

```python
@router.get("/new-endpoint")
async def new_endpoint(param: str):
    """
    Описание endpoint.

    Args:
        param: Параметр запроса

    Returns:
        Результат
    """
    result = search_service.some_method(param)
    return result
```

### Тестирование

```bash
# Запуск тестов (если есть)
pytest

# Проверка типов
mypy app/

# Линтинг
flake8 app/
```

### Логирование

Сервис использует стандартное логирование Python. Для настройки:

```python
import logging

logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s'
)
```

## Производительность

### Оптимизация запросов

1. **Используйте фильтры** - они быстрее, чем запросы
2. **Ограничивайте size** - не запрашивайте больше, чем нужно
3. **Кэшируйте результаты** - для часто используемых запросов
4. **Используйте конкретные поля** - вместо поиска по всем полям

### Масштабирование

- Используйте несколько workers в продакшне
- Настройте connection pooling для OpenSearch
- Рассмотрите использование Redis для кэширования
- Используйте load balancer для распределения нагрузки

## Безопасность

### Рекомендации

1. **Не храните секреты в коде** - используйте переменные окружения
2. **Ограничьте CORS** - укажите конкретные origins в продакшне
3. **Используйте HTTPS** - настройте SSL/TLS
4. **Rate limiting** - ограничьте количество запросов
5. **Аутентификация** - добавьте API ключи или OAuth

## Troubleshooting

### Проблемы с подключением к OpenSearch

```python
# Проверьте настройки в app/config.py
# Убедитесь, что OpenSearch запущен
curl https://localhost:9200 -u admin:password
```

### Ошибки поиска

- Проверьте, что индекс существует: `GET /health`
- Проверьте формат запроса в Swagger UI
- Проверьте логи сервиса

### Медленные запросы

- Уменьшите `size` параметр
- Используйте фильтры вместо запросов
- Проверьте производительность OpenSearch
