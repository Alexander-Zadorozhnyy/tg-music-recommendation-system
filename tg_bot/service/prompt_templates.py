RELEVANCE_PROMPT = """Ты — строгий фильтр запросов. Ответь ТОЛЬКО "ДА", если запрос связан с музыкой, песнями, исполнителями, рекомендациями музыки или поиском треков. Во всех остальных случаях ответь ТОЛЬКО "НЕТ".

Запрос: "{query}"

Ответ:"""


NORMALIZE_TRACKS_PROMPT = """Ты — музыкальный эксперт русской и английской музыки. Исправь опечатки в исполнителях и названиях песен.

ПРАВИЛА:
1. Верни ТОЛЬКО валидный JSON-массив в формате:
[{{"artist": "Исправленный Исполнитель", "song": "Исправленная Песня"}}, ...]
2. НЕ добавляй пояснения, комментарии, текст до или после JSON.
3. Если трек неузнаваемый — пропусти его (не включай в массив).
4. Используй оригинальные названия на русском и английском, если известны.

Вход:
{tracks}

Выход (только JSON):"""
